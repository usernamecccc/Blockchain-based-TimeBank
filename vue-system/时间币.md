# 1.注册登录模块

## 1.1 注册

### 1.1.1 请求路径

/register

### 1.1.2 请求方式

POST

### 1.1.3 接口描述

用于注册不同类型的用户

### 1.1.4 请求数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                                          |      |
| -------- | -------- | -------- | --------------------------------------------- | ---- |
| username | string   | 必须     | 用户名                                        |      |
| name     | string   | 必须     | 真实姓名                                      |      |
| password | string   | 必须     | 密码                                          |      |
| role     | number   | 必须     | 用户类型，1代表老人，2代表志愿者，3代表管理员 |      |
| email    | string   | 非必须   | 邮箱                                          |      |
| age      | number   | 必须     | 年龄                                          |      |
| phone    | string   | 非必须   | 电话号码                                      |      |
| address  | string   | 非必须   | 地方                                          |      |

请求数据样例：

```json
{
    "username":"tang",
    "name":"唐"
    "password":"123456",
    "role":1,
    "email":"123@qq.com",
    "age":12,
    "phone":"124324",
    "address":"重庆市"
}
```

### 1.1.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                         |
| -------- | -------- | -------- | ---------------------------- |
| code     | number   | 必须     | 响应码，1代表成功，0代表失败 |
| msg      | string   | 非必须   | 提示信息                     |
| data     | object   | 非必须   | 返回的数据                   |

响应数据样例：

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

## 1.2 登录

### 1.2.1 请求路径

/login

### 1.2.2 请求方式

POST

### 1.2.3 接口描述

用于员工登录系统，登录完毕后，下发jwt令牌

### 1.2.4 请求数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注   |
| -------- | -------- | -------- | ------ |
| username | string   | 必须     | 用户名 |
| password | string   | 必须     | 密码   |

样例：

```json
{
    "username":"tang",
    "password":"123"
    ""
}
```

### 1.2.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     | 返回的数据，jwt令牌  |

样例：

```json
{
    "code":1,
    "msg":"success",
    "data":{
        "token":"fhjasuifewq",
        "role":1
	}

}
```

用户登录失败的错误信息

```json
{
	"code":0,
    "msg":"NOT_LOGIN",
    "data":null
}
```

用户访问不对应的页面时

```json
{
	"code":0,
    "msg":"ACCESS_RESTRICTED",
    "data":null
}
```



# 2.管理员模块

## 2.1 查询活动

### 2.1.1 请求路径

/administrator

### 2.1.2 请求方式

GET

### 2.1.3 接口描述

用于分页和分类查询活动

### 2.1.4 请求数据

格式：queryString

| 参数名称 | 是否必传 | 示例       | 备注                                                         |
| -------- | -------- | ---------- | ------------------------------------------------------------ |
| title    | 否       | 打扫       | 根据活动标题模糊匹配查询                                     |
| date     | 否       | 2022-01-02 | 根据活动日期查询                                             |
| begin    | 否       | 12:34:32   | 根据活动开始时间查询，时分秒                                 |
| end      | 否       | 12:43:12   | 根据活动结束时间查询，在begin和end时间段类的都能查询         |
| status   | 否       | 1          | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动，5代表未发布 |
| address  | 否       | 重庆市     | 根据地址模糊匹配                                             |
| page     | 是       | 1          | 分页查询的页码，如果未指定，默认为1                          |
| pageSize | 是       | 10         | 分页查询的每页记录数，如果未指定，默认为10                   |

样例：

```
/administrator?title=打扫&date=2022-1-2&page=1&pageSize=10
```

### 2.1.5 响应数据

格式：json

| 参数名称           | 参数类型 | 是否必传 | 备注                                                         |
| ------------------ | -------- | -------- | ------------------------------------------------------------ |
| code               | number   | 必须     | 响应码，1成功，0失败                                         |
| msg                | string   | 必须     | 提示信息                                                     |
| data               | object   | 必须     | 返回的数据                                                   |
| \|-total           | number   | 必须     | 总记录数                                                     |
| \|-rows            | object[] | 必须     | 每个活动的数据包装进去                                       |
| \|-id              | number   | 非必须   | 活动的id                                                     |
| \|-title           | string   | 非必须   | 志愿者活动标题                                               |
| \|-quota           | string   | 非必须   | 活动名额                                                     |
| \|-deadline        | string   | 非必须   | 活动报名截止时间                                             |
| \|-date            | string   | 非必须   | 活动日期                                                     |
| \|-begin           | string   | 非必须   | 活动开始时间，时分秒                                         |
| \|-end             | string   | 非必须   | 活动结束时间，时分秒                                         |
| \|-address         | string   | 非必须   | 活动地址                                                     |
| \|-oldId           | number   | 非必须   | (id的大写Id)，发布活动的老人id                               |
| \|-phone           | string   | 非必须   | 发布人的电话号码                                             |
| \|-description     | string   | 非必须   | 对于该志愿活动的描述                                         |
| \|-status          | number   | 非必须   | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动，5代表未发布 |
| \|-administratorId | number   | 非必须   | 如果审核了，管理员的管理员id                                 |
| \|-createTime      | string   | 非必须   | 创建时间                                                     |
| \|-updateTime      | string   | 非必须   | 更新时间                                                     |
| \|-message         | string   | 非必须   | 管理员对项目的意见，主要对不同意的活动                       |
| \|-remain          | number   | 非必须   | 活动剩余名额                                                 |

样例：

```json
{
    "code":1,
    "msg":"success",
    "data":{
        "total":2,
        "row":[
            {
				"id":"1",
                ...省略了,
                "message":"这个用户活动不对劲"
            }
            {
            	"id":"2",
                ...省略了,
                "message":"这个用户活动不对劲"
            }
            
        ]
	}

}
```

## 2.2 删除活动

### 2.2.1 请求路径

/administrator/{ids}

注：这是活动的id

### 2.2.2 请求方式

DELETE

### 2.2.3 接口描述

用于删除活动

### 2.2.4 请求数据

格式：路径参数

| 参数名 | 类型      | 示例  | 是否必须 | 备注         |
| ------ | --------- | ----- | -------- | ------------ |
| ids    | 数组array | 1,2,3 | 必须     | 活动的id数组 |

### 2.2.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     |                      |

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

## 2.3 修改活动

### 2.3.1 请求路径

/administrator

### 2.3.2 请求方式

PUT

### 2.3.3 接口描述

修改活动信息

### 2.3.4 请求数据

格式json

| 参数名称        | 参数类型 | 是否必传 | 备注                                                         |
| --------------- | -------- | -------- | ------------------------------------------------------------ |
| id              | number   | 非必须   | 活动的id                                                     |
| title           | string   | 非必须   | 志愿者活动标题                                               |
| quota           | string   | 非必须   | 活动名额                                                     |
| deadline        | string   | 非必须   | 活动报名截止时间                                             |
| date            | string   | 非必须   | 活动日期                                                     |
| begin           | string   | 非必须   | 活动开始时间，时分秒                                         |
| end             | string   | 非必须   | 活动结束时间，时分秒                                         |
| address         | string   | 非必须   | 活动地址                                                     |
| oldId           | number   | 非必须   | (id的大写Id)，发布活动的老人id                               |
| phone           | string   | 非必须   | 发布人的电话号码                                             |
| description     | string   | 非必须   | 对于该志愿活动的描述                                         |
| status          | number   | 非必须   | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动，5代表未发布 |
| administratorId | number   | 非必须   | 如果审核了，管理员的管理员id                                 |
| message         | string   | 非必须   | 管理员对项目的意见，主要对不同意的活动                       |
| remain          | number   | 非必须   | 活动剩余名额                                                 |

示例：

```json
{
	"id":"1",
	 ...省略了,
	"message":"这个用户活动不对劲",
    "remain":"123"
}
```



### 2.3.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     |                      |

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

## 2.4 新增活动

### 2.4.1 请求路径

/administrator

### 2.4.2 请求方式

POST

### 2.4.3 接口描述

新增加活动信息

### 2.4.4 请求数据

json格式

| 参数名称        | 参数类型 | 是否必传 | 备注                                                         |
| --------------- | -------- | -------- | ------------------------------------------------------------ |
| title           | string   | 必须     | 志愿者活动标题                                               |
| quota           | string   | 必须     | 活动名额                                                     |
| deadline        | string   | 必须     | 活动报名截止时间                                             |
| date            | string   | 必须     | 活动日期                                                     |
| begin           | string   | 必须     | 活动开始时间，时分秒                                         |
| end             | string   | 必须     | 活动结束时间，时分秒                                         |
| address         | string   | 必须     | 活动地址                                                     |
| oldId           | number   | 必须     | (id的大写Id)，发布活动的老人id                               |
| phone           | string   | 必须     | 发布人的电话号码                                             |
| description     | string   | 必须     | 对于该志愿活动的描述                                         |
| status          | number   | 必须     | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动，5代表未发布 |
| administratorId | number   | 必须     | 如果审核了，管理员的管理员id                                 |
| message         | string   | 非必须   | 管理员对项目的意见，主要对不同意的活动                       |

示例

```json
{
	"id":"1",
	 ...省略了,
	"message":"这个用户活动不对劲"
}
```



### 2.4.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     |                      |

示例：

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

## 2.5 新增加用户

### 2.5.1 请求路径

/administrator/users

### 2.5.2 请求方式

POST

### 2.5.3 接口描述

管理员新增加用户

### 2.5.4 请求数据

请求数据格式：JSON

| 参数名称 | 参数类型 | 是否必传 | 备注                                          |
| -------- | -------- | -------- | --------------------------------------------- |
| username | string   | 必须     | 用户名，最大长度为20字符                      |
| password | string   | 必须     | 密码，最大长度为20字符                        |
| role     | number   | 必须     | 用户类型，1代表老人，2代表志愿者，3代表管理员 |
| email    | string   | 非必须   | 电子邮箱，最大长度为30字符                    |
| age      | number   | 必须     | 用户年龄                                      |
| phone    | string   | 非必须   | 电话号码，最大长度为20字符                    |
| address  | string   | 非必须   | 地址，最大长度为30字符                        |
| name     | string   | 必须     | 姓名，最大长度为20字符                        |

```json
{
    "username": "example_user",
    "password": "example_password",
    "role": "user",
    "email": "example@example.com",
    "age": 25,
    "phone": "1234567890",
    "address": "123 Example St",
    "name": "John Doe"
}

```

### 2.5.5 响应数据

响应数据格式：JSON

| 参数名称 | 参数类型 | 是否必传 | 备注                         |
| -------- | -------- | -------- | ---------------------------- |
| code     | number   | 必须     | 响应码，1代表成功，0代表失败 |
| msg      | string   | 非必须   | 提示信息                     |
| data     | object   | 非必须   | 返回的数据                   |

响应数据样例：

```json
{
    "code": 1,
    "msg": "success",
    "data": null
}
```

## 2.6 修改或更新用户信息

### 2.6.1 请求路径

/administrator/users

### 2.6.2 请求方式

PUT

### 2.6.3 接口描述

管理员修改或更新用户信息，为null的值不会修改

### 2.6.4 请求数据

请求数据格式：JSON

| 参数名称 | 参数类型 | 是否必传 | 备注                                          |
| -------- | -------- | -------- | --------------------------------------------- |
| id       | number   | 必须     | 用户id                                        |
| username | string   | 非必须   | 用户名，最大长度为20字符                      |
| password | string   | 非必须   | 密码，最大长度为20字符                        |
| role     | number   | 非必须   | 用户类型，1代表老人，2代表志愿者，3代表管理员 |
| email    | string   | 非必须   | 电子邮箱，最大长度为30字符                    |
| age      | number   | 非必须   | 用户年龄                                      |
| phone    | string   | 非必须   | 电话号码，最大长度为20字符                    |
| address  | string   | 非必须   | 地址，最大长度为30字符                        |
| name     | string   | 非必须   | 姓名，最大长度为20字符                        |
| image    | string   | 非必须   | 图片地址，最大长度为300字符                   |

```json
{
    "id":1,
    "username": "example_user",
    "password": "example_password",
    "role": "user",
    "email": "example@example.com",
    "age": 25,
    "phone": "1234567890",
    "address": "123 Example St",
    "name": "John Doe",
    "image":"fdjauifheuiw"
}

```

### 2.6.5 响应数据

响应数据格式：JSON

| 参数名称 | 参数类型 | 是否必传 | 备注                         |
| -------- | -------- | -------- | ---------------------------- |
| code     | number   | 必须     | 响应码，1代表成功，0代表失败 |
| msg      | string   | 非必须   | 提示信息                     |
| data     | object   | 非必须   | 返回的数据                   |

响应数据样例：

```json
{
    "code": 1,
    "msg": "success",
    "data": null
}
```

若用户名重复

```json
{
    "code": 0,
    "msg": "修改的用户名已存在",
    "data": null
}
```



## 2.7 删除用户信息

### 2.7.1 请求路径

/administrator/users/{ids}

### 2.7.2 请求方式

DELETE

### 2.7.3 接口描述

管理员批量删除用户

### 2.7.4 请求数据

格式：路径参数

| 参数名 | 类型      | 示例  | 是否必须 | 备注     |
| ------ | --------- | ----- | -------- | -------- |
| ids    | 数组array | 1,2,3 | 必须     | 用户的id |

### 2.7.5 响应数据

响应数据格式：JSON

| 参数名称 | 参数类型 | 是否必传 | 备注                         |
| -------- | -------- | -------- | ---------------------------- |
| code     | number   | 必须     | 响应码，1代表成功，0代表失败 |
| msg      | string   | 非必须   | 提示信息                     |
| data     | object   | 非必须   | 返回的数据                   |

响应数据样例：

```json
{
    "code": 1,
    "msg": "success",
    "data": null
}
```

## 2.8 查询用户信息

### 2.8.1 请求路径

/administrator/users

### 2.8.2 请求方式

GET

### 2.8.3 接口描述

管理员查询用户

### 2.8.4 请求数据

格式：queryString

| 参数名称 | 是否必传 | 示例   | 备注                                                         |
| -------- | -------- | ------ | ------------------------------------------------------------ |
| page     | 是       | 1      | 分页查询的页码，如果未指定，默认为1                          |
| pageSize | 是       | 10     | 分页查询的每页记录数，如果未指定，默认为10                   |
| role     | 否       | 1      | 用户类型，1代表老人，2代表志愿者，3代表管理员                |
| username | 否       | jfsida | 用户名，到时候查询也是模糊查询，只要是连续的相同字符串都成立 |
| address  | 否       | 重庆   | 只要含有重庆儿子的模糊查询                                   |
| minAge   | 否       | 12     | 最小年龄                                                     |
| maxAge   | 否       | 32     | 最大年龄                                                     |

示例：/administrator/users?page=1&pageSize=10&role=1.....

### 2.8.5 响应数据

响应数据格式：JSON

| 参数名称      | 参数类型 | 是否必传 | 备注                                          |
| ------------- | -------- | -------- | --------------------------------------------- |
| code          | number   | 必须     | 响应码，1成功，0失败                          |
| msg           | string   | 必须     | 提示信息                                      |
| data          | object   | 必须     | 返回的数据                                    |
| \|-total      | number   | 必须     | 总记录数                                      |
| \|-rows       | object[] | 必须     | 每个活动的数据包装进去                        |
| \|-id         | number   | 必须     | 用户id                                        |
| \|-username   | string   | 必须     | 用户名，最大长度为20字符                      |
| \|-password   | string   | 必须     | 密码，最大长度为20字符                        |
| \|-role       | number   | 必须     | 用户类型，1代表老人，2代表志愿者，3代表管理员 |
| \|-email      | string   | 必须     | 电子邮箱，最大长度为30字符                    |
| \|-age        | number   | 必须     | 用户年龄                                      |
| \|-phone      | string   | 必须     | 电话号码，最大长度为20字符                    |
| \|-address    | string   | 必须     | 地址，最大长度为30字符                        |
| \|-name       | string   | 非必须   | 姓名，最大长度为20字符                        |
| \|-createTime | string   | 非必须   | 创建时间                                      |
| \|-updateTime | string   | 非必须   | 更新时间                                      |
| \|-image      | string   | 非必须   | 图片                                          |

响应数据样例：

```json
{
    "code": 1,
    "msg": "success",
    "data": {
        "total":2,
        "row":[
            {
        		"id": 123,
       			"username": "example_user",
        		"role": "user",
        		"email": "example@example.com",
        		"age": 25,
        		"phone": "1234567890",
        		"address": "123 Example St",
        		"name": "John Doe",
        		"createTime": "2024-03-11T12:00:00",
        		"updateTime": "2024-03-11T12:00:00Z",
                "image":''
    		}
            {
            	"id": 123,
       			"username": "example_user",
        		"role": "user",
        		"email": "example@example.com",
        		"age": 25,
        		"phone": "1234567890",
        		"address": "123 Example St",
        		"name": "John Doe",
        		"createTime": "2024-03-11T12:00:00",
        		"updateTime": "2024-03-11T12:00:00",
            	"image":''
            }       
        ]
	}
}
```

# 3.用户操作管理模块

## 3.1 老人查询自己的活动

### 3.1.1 请求路径

/users/old

### 3.1.2 请求方式

GET

### 3.1.3 接口描述

用于分页和分类查询老人自己的活动

### 3.1.4 请求数据

格式：queryString

| 参数名称 | 是否必传 | 示例 | 备注                                       |
| -------- | -------- | ---- | ------------------------------------------ |
| page     | 是       | 1    | 分页查询的页码，如果未指定，默认为1        |
| pageSize | 是       | 10   | 分页查询的每页记录数，如果未指定，默认为10 |

样例：

```
/administrator/old?page=1&pageSize=10
```

### 3.1.5 响应数据

格式：json

| 参数名称           | 参数类型 | 是否必传 | 备注                                                         |
| ------------------ | -------- | -------- | ------------------------------------------------------------ |
| code               | number   | 必须     | 响应码，1成功，0失败                                         |
| msg                | string   | 必须     | 提示信息                                                     |
| data               | object   | 必须     | 返回的数据                                                   |
| \|-total           | number   | 必须     | 总记录数                                                     |
| \|-rows            | object[] | 必须     | 每个活动的数据包装进去                                       |
| \|-id              | number   | 非必须   | 活动的id                                                     |
| \|-title           | string   | 非必须   | 志愿者活动标题                                               |
| \|-quota           | string   | 非必须   | 活动名额                                                     |
| \|-deadline        | string   | 非必须   | 活动报名截止时间                                             |
| \|-date            | string   | 非必须   | 活动日期                                                     |
| \|-begin           | string   | 非必须   | 活动开始时间，时分秒                                         |
| \|-end             | string   | 非必须   | 活动结束时间，时分秒                                         |
| \|-address         | string   | 非必须   | 活动地址                                                     |
| \|-oldId           | number   | 非必须   | (id的大写Id)，发布活动的老人id                               |
| \|-phone           | string   | 非必须   | 发布人的电话号码                                             |
| \|-description     | string   | 非必须   | 对于该志愿活动的描述                                         |
| \|-status          | number   | 非必须   | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动 |
| \|-administratorId | number   | 非必须   | 如果审核了，管理员的管理员id                                 |
| \|-createTime      | string   | 非必须   | 创建时间                                                     |
| \|-updateTime      | string   | 非必须   | 更新时间                                                     |
| \|-message         | string   | 非必须   | 管理员对项目的意见，主要对不同意的活动                       |
| \|-remain          | number   | 非必须   | 活动剩余名额                                                 |

样例：

```json
{
    "code":1,
    "msg":"success",
    "data":{
        "total":2,
        "row":[
            {
				"id":"1",
                ...省略了,
                "message":"这个用户活动不对劲"
            }
            {
            	"id":"2",
                ...省略了,
                "message":"这个用户活动不对劲"
            }
            
        ]
	}

}
```

## 3.2 老人新增加活动

### 3.2.1 请求路径

/users/old

### 3.2.2 请求方式

POST

### 3.2.3 接口描述

老人新创建活动，开始状态默认为5（未发布），如果要发布就用修改活动改状态status为1，remain字段后端（我）会初始化未quota（活动名额）

### 3.2.4 请求数据

| 参数名称        | 参数类型 | 是否必传 | 备注                                                         |
| --------------- | -------- | -------- | ------------------------------------------------------------ |
| title           | string   | 必须     | 志愿者活动标题                                               |
| quota           | string   | 必须     | 活动名额                                                     |
| deadline        | string   | 必须     | 活动报名截止时间                                             |
| date            | string   | 必须     | 活动日期                                                     |
| begin           | string   | 必须     | 活动开始时间，时分秒                                         |
| end             | string   | 必须     | 活动结束时间，时分秒                                         |
| address         | string   | 必须     | 活动地址                                                     |
| phone           | string   | 必须     | 发布人的电话号码                                             |
| description     | string   | 必须     | 对于该志愿活动的描述                                         |
| status          | number   | 非必须   | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动，5代表未发布 |
| administratorId | number   | 非必须   | 如果审核了，管理员的管理员id                                 |
| message         | string   | 非必须   | 管理员对项目的意见，主要对不同意的活动                       |

```json
{
	"id":"1",
	 ...省略了,
	"message":"这个用户活动不对劲"
}
```



### 3.2.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     |                      |

示例：

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

## 3.3 老人修改或更新活动数据

### 3.3.1 请求路径

/users/old

### 3.3.2 请求方式

PUT

### 3.3.3 接口描述

修改活动信息

### 3.3.4 请求数据

格式json

| 参数名称        | 参数类型 | 是否必传 | 备注                                                         |
| --------------- | -------- | -------- | ------------------------------------------------------------ |
| id              | number   | 非必须   | 活动的id                                                     |
| title           | string   | 非必须   | 志愿者活动标题                                               |
| quota           | string   | 非必须   | 活动名额                                                     |
| deadline        | string   | 非必须   | 活动报名截止时间                                             |
| date            | string   | 非必须   | 活动日期                                                     |
| begin           | string   | 非必须   | 活动开始时间，时分秒                                         |
| end             | string   | 非必须   | 活动结束时间，时分秒                                         |
| address         | string   | 非必须   | 活动地址                                                     |
| oldId           | number   | 非必须   | (id的大写Id)，发布活动的老人id                               |
| phone           | string   | 非必须   | 发布人的电话号码                                             |
| description     | string   | 非必须   | 对于该志愿活动的描述                                         |
| status          | number   | 非必须   | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动，5代表未发布 |
| administratorId | number   | 非必须   | 如果审核了，管理员的管理员id                                 |
| message         | string   | 非必须   | 管理员对项目的意见，主要对不同意的活动                       |
| remain          | number   | 非必须   | 活动剩余名额                                                 |

示例：

```json
{
	"id":"1",
	 ...省略了,
	"message":"这个用户活动不对劲",
    "remain":"123"
}
```



### 3.3.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     |                      |

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

## 3.4 老人删除活动

### 3.4.1 请求路径

/users/old/{ids}

注：这是活动的id

### 3.4.2 请求方式

DELETE

### 3.4.3 接口描述

用于删除活动

### 3.4.4 请求数据

格式：路径参数

| 参数名 | 类型      | 示例  | 是否必须 | 备注         |
| ------ | --------- | ----- | -------- | ------------ |
| ids    | 数组array | 1,2,3 | 必须     | 活动的id数组 |

### 3.4.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     |                      |

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

## 3.5 志愿者用户参加或报名活动

### 3.5.1 请求路径

/users/vol

### 3.5.2 请求方式

PUT

### 3.5.3 接口描述

用户报名活动，把id给后端，前端利用查询的数据，把remain剩余位置展示给用户。报名时名额不够了，后端会提示错误信息。

### 3.5.4 请求数据

格式json

| 参数名称 | 参数类型 | 是否必传 | 备注     |
| -------- | -------- | -------- | -------- |
| id       | number   | 非必须   | 活动的id |

示例：

```json
{
	"id":1
}
```



### 3.5.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     |                      |

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

名额不够时：

```json
{
    "code":0,
    "msg":"剩余名额不够",
    "data":null
}
```



## 3.6 志愿者用户查询可接受活动

### 3.6.1 请求路径

/users/vol

### 3.6.2 请求方式

GET

### 3.6.3 接口描述

用于分页和分类查询可接受的任务，status为2（已经审核的活动）

且用户没有报名的活动

### 3.6.4 请求数据

格式：queryString

| 参数名称 | 是否必传 | 示例                | 备注                                                         |
| -------- | -------- | ------------------- | ------------------------------------------------------------ |
| title    | 否       | 打扫                | 根据活动标题模糊匹配查询                                     |
| date     | 否       | 2022-01-02          | 根据活动日期查询                                             |
| begin    | 否       | 12:34:32            | 根据活动开始时间查询，时分秒                                 |
| end      | 否       | 12:43:12            | 根据活动结束时间查询，在begin和end时间段类的都能查询         |
| address  | 否       | 重庆市              | 根据地址模糊匹配                                             |
| page     | 是       | 1                   | 分页查询的页码，如果未指定，默认为1                          |
| pageSize | 是       | 10                  | 分页查询的每页记录数，如果未指定，默认为10                   |
| deadline | 否       | 2313-12-13 12:23:12 | 我会查询你发给我的deadline（你给我的时间）<=数据库的deadline（报名截止） |
| status   | 否       | 2                   | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动 |

样例：

```
/vol?page=1&pageSize=10&title=打扫
```

### 3.6.5 响应数据

格式：json

| 参数名称           | 参数类型 | 是否必传 | 备注                                                         |
| ------------------ | -------- | -------- | ------------------------------------------------------------ |
| code               | number   | 必须     | 响应码，1成功，0失败                                         |
| msg                | string   | 必须     | 提示信息                                                     |
| data               | object   | 必须     | 返回的数据                                                   |
| \|-total           | number   | 必须     | 总记录数                                                     |
| \|-rows            | object[] | 必须     | 每个活动的数据包装进去                                       |
| \|-id              | number   | 非必须   | 活动的id                                                     |
| \|-title           | string   | 非必须   | 志愿者活动标题                                               |
| \|-quota           | string   | 非必须   | 活动名额                                                     |
| \|-deadline        | string   | 非必须   | 活动报名截止时间                                             |
| \|-date            | string   | 非必须   | 活动日期                                                     |
| \|-begin           | string   | 非必须   | 活动开始时间，时分秒                                         |
| \|-end             | string   | 非必须   | 活动结束时间，时分秒                                         |
| \|-address         | string   | 非必须   | 活动地址                                                     |
| \|-oldId           | number   | 非必须   | (id的大写Id)，发布活动的老人id                               |
| \|-phone           | string   | 非必须   | 发布人的电话号码                                             |
| \|-description     | string   | 非必须   | 对于该志愿活动的描述                                         |
| \|-status          | number   | 非必须   | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动 |
| \|-administratorId | number   | 非必须   | 如果审核了，管理员的管理员id                                 |
| \|-createTime      | string   | 非必须   | 创建时间                                                     |
| \|-updateTime      | string   | 非必须   | 更新时间                                                     |
| \|-message         | string   | 非必须   | 管理员对项目的意见，主要对不同意的活动                       |
| \|-remain          | number   | 非必须   | 活动剩余名额                                                 |

样例：

```json
{
    "code":1,
    "msg":"success",
    "data":{
        "total":2,
        "row":[
            {
				"id":"1",
                ...省略了,
                "message":"这个用户活动不对劲"
            }
            {
            	"id":"2",
                ...省略了,
                "message":"这个用户活动不对劲"
            }
            
        ]
	}

}
```

## 3.7 志愿者用户查询自己报名的活动

### 3.7.1 请求路径

users/vol/activity

### 3.7.2 请求方式

GET

### 3.7.3 接口描述

用户查询自己报名过的活动

### 3.7.4 请求数据

格式：queryString

| 参数名称 | 是否必传 | 示例       | 备注                                                         |
| -------- | -------- | ---------- | ------------------------------------------------------------ |
| title    | 否       | 打扫       | 根据活动标题模糊匹配查询                                     |
| date     | 否       | 2022-01-02 | 根据活动日期查询                                             |
| begin    | 否       | 12:34:32   | 根据活动开始时间查询，时分秒                                 |
| end      | 否       | 12:43:12   | 根据活动结束时间查询，在begin和end时间段类的都能查询         |
| address  | 否       | 重庆市     | 根据地址模糊匹配                                             |
| status   | 否       | 1          | 1代表未审核，2代表审核同意，3代表审核不同意，4代表已过期，5代表未发布，主要根据4和2来区分过期否 |
| page     | 是       | 1          | 分页查询的页码，如果未指定，默认为1                          |
| pageSize | 是       | 10         | 分页查询的每页记录数，如果未指定，默认为10                   |

样例：

```
/vol?page=1&pageSize=10&title=打扫
```

### 3.7.5 响应数据

格式：json

| 参数名称           | 参数类型 | 是否必传 | 备注                                                         |
| ------------------ | -------- | -------- | ------------------------------------------------------------ |
| code               | number   | 必须     | 响应码，1成功，0失败                                         |
| msg                | string   | 必须     | 提示信息                                                     |
| data               | object   | 必须     | 返回的数据                                                   |
| \|-total           | number   | 必须     | 总记录数                                                     |
| \|-rows            | object[] | 必须     | 每个活动的数据包装进去                                       |
| \|-id              | number   | 非必须   | 活动的id                                                     |
| \|-title           | string   | 非必须   | 志愿者活动标题                                               |
| \|-quota           | string   | 非必须   | 活动名额                                                     |
| \|-deadline        | string   | 非必须   | 活动报名截止时间                                             |
| \|-date            | string   | 非必须   | 活动日期                                                     |
| \|-begin           | string   | 非必须   | 活动开始时间，时分秒                                         |
| \|-end             | string   | 非必须   | 活动结束时间，时分秒                                         |
| \|-address         | string   | 非必须   | 活动地址                                                     |
| \|-oldId           | number   | 非必须   | (id的大写Id)，发布活动的老人id                               |
| \|-phone           | string   | 非必须   | 发布人的电话号码                                             |
| \|-description     | string   | 非必须   | 对于该志愿活动的描述                                         |
| \|-status          | number   | 非必须   | 1代表未审核，2代表审核同意，3代表审核不同意，4代表已过期，5代表未发布，主要根据4和2来区分过期否 |
| \|-administratorId | number   | 非必须   | 如果审核了，管理员的管理员id                                 |
| \|-createTime      | string   | 非必须   | 创建时间                                                     |
| \|-updateTime      | string   | 非必须   | 更新时间                                                     |
| \|-message         | string   | 非必须   | 管理员对项目的意见，主要对不同意的活动                       |
| \|-remain          | number   | 非必须   | 活动剩余名额                                                 |

样例：

```json
{
    "code":1,
    "msg":"success",
    "data":{
        "total":2,
        "row":[
            {
				"id":"1",
                ...省略了,
                "message":"这个用户活动不对劲",
                "remain":12
            }
            {
            	"id":"2",
                ...省略了,
                "message":"这个用户活动不对劲",
            	"remain":132
            }
            
        ]
	}

}
```

## 3.8 志愿者取消活动报名

### 3.8.1 请求路径

users/vol/cancel

### 3.8.2 请求方式

PUT

### 3.8.3 接口描述

志愿者用户取消活动报名

### 3.8.4 请求数据

格式json

| 参数名称 | 参数类型 | 是否必传 | 备注     |
| -------- | -------- | -------- | -------- |
| id       | number   | 非必须   | 活动的id |

示例：

```json
{
	"id":1
}
```

### 3.8.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     |                      |

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

出问题时：

```json
{
    "code":0,
    "msg":"退出活动失败",
    "data":null
}
```

## 3.9 老人查看当前活动，所有参与的志愿者的完成状态和签到状态

### 3.9.1 请求路径

/users/old/status

### 3.9.2 请求方式

GET

### 3.9.3 接口描述

老人查询当前该活动所有的志愿者信息，包括姓名，电话，邮箱，年龄，完成状态和签到状态

### 3.9.4 请求数据

格式：queryString

| 参数名称 | 是否必传 | 示例 | 备注       |
| -------- | -------- | ---- | ---------- |
| id       | 是       | 1    | 活动id     |
| pageSize | 是       | 10   | 一页的个数 |
| page     | 是       | 1    | 当前页数   |

样例：

```
/users/old/status?page=1&pageSize=10&id=1
```

### 3.9.5 响应数据

格式：json

| 参数名称  | 参数类型 | 是否必传 | 备注                             |
| --------- | -------- | -------- | -------------------------------- |
| code      | number   | 必须     | 响应码，1成功，0失败             |
| msg       | string   | 非必须   | 提示信息                         |
| data      | string   | 必须     | 数据                             |
| \|-total  | number   | 必须     | 总人数                           |
| \|-rows   | object[] | 必须     | 将数据包装进去                   |
| \|-id     | number   | 必须     | 用户id                           |
| \|-name   | string   | 必须     | 志愿者名字                       |
| \|-phone  | string   | 必须     | 志愿者电话                       |
| \|-email  | string   | 必须     | 志愿者邮箱                       |
| \|-age    | number   | 必须     | 志愿者年龄                       |
| \|-status | number   | 必须     | 完成状态，0代表未完成，1代表完成 |
| \|-sign   | number   | 必须     | 签到状态，0代表未签到，1代表签到 |

```json
{
    "code":1,
    "msg":"success",
    "data":{"total":2,
       		"row":[
            {
				"id":1,
                "name":"1",
                "phone":"fdsa",
                "email":"dfas"，
    			"age":1,
                "status":1,
                "sign":1
            }
            {
				"id":1,
                "name":"1",
                "phone":"fdsa",
                "email":"dfas"，
    			"age":1,
                "status":1,
                "sign":1
            }
            ]
		}
}
```

## 3.10 老人更改志愿者的完成状态和签到状态

### 3.10.1 请求路径

/users/old/status

### 3.10.2 请求方式

PUT

### 3.10.3 接口描述

提供用户id，老人更改志愿者的完成状态和签到状态

### 3.10.4 请求数据

格式：json

| 参数名称   | 是否必传 | 示例 | 备注                   |
| ---------- | -------- | ---- | ---------------------- |
| userId     | 是       | 1    | 志愿者用户id           |
| activityId | 是       | 1    | 活动id                 |
| sign       | 否       | 1    | 0代表未签到，1代表签到 |
| status     | 否       | 1    | 0代表未完成，1代表完成 |

### 3.10.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     | 数据                 |

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

## 3.11 志愿者用户进行签到

### 3.11.1 请求路径

users/vol/sign

### 3.11.2 请求方式

PUT

### 3.11.3 接口描述

志愿者用户进行定位检查签到

### 3.11.4 请求数据

格式：json

| 参数名称 | 是否必传 | 示例 | 备注       |
| -------- | -------- | ---- | ---------- |
| id       | 是       | 1    | 活动id     |
| address  | 否       | 1    | 活动的地址 |

### 3.11.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     | 数据                 |

```json
{
    "code":1,
    "msg":"success",
    "data":null
}
```

如果签到失败

```json
{
    "code":0,
    "msg":"签到失败，地址错误",
    "data":null
}
```

## 3.12 志愿者用户查询自己某个活动的签到和完成信息

### 3.12.1 请求路径

users/vol/sign

### 3.12.2 请求方式

GET

### 3.12.3 接口描述

志愿者查询自己报名了的活动对应签到和完成状态

### 3.12.4 请求数据

| 参数名称   | 是否必传 | 示例 | 备注   |
| ---------- | -------- | ---- | ------ |
| activityId | 是       | 1    | 活动id |

### 3.12.5 响应数据

格式：json

| 参数名称   | 参数类型 | 是否必传 | 备注                                                 |
| ---------- | -------- | -------- | ---------------------------------------------------- |
| code       | number   | 必须     | 响应码，1成功，0失败                                 |
| msg        | string   | 非必须   | 提示信息                                             |
| data       | number   | 必须     | 数据                                                 |
| id         | number   | 必须     | 中间表主键不用管                                     |
| volId      | number   | 必须     | 用户的志愿者id（不用管，封装在类里的，一起发过来了） |
| userId     | number   | 必须     | 用户id                                               |
| activityId | number   | 必须     | 活动id                                               |
| status     | number   | 必须     | 活动完成状态，0为未完成，1为完成                     |
| sign       | number   | 必须     | 活动签到状态，0为未签到，1为签到                     |
| createTime | string   | 必须     | 创建时间，含义为用户报名该活动时的时间，年月日时分秒 |
| updateTime | string   | 必须     | 发生更改的时间，年月日时分秒                         |

```json
{
    "code":1,
    "msg":"success",
    "data":{
        "id":12,
        "userId":12,
        "volId":1,
        "activityId":1,
        "status":1,
        "sign":1,
        "createTime":"",
        "updateTime":""
    }
}
```



# 4. 用户个人信息管理模块

## 4.1 查看个人信息

### 4.1.1 请求路径

/info

### 4.1.2 请求方式

GET

### 4.1.3 接口描述

查询个人信息。，登录后才可用

### 4.1.4 请求数据

无

### 4.1.5 响应数据

请求数据格式：JSON

| 参数名称 | 参数类型     | 是否必传 | 备注                       |
| -------- | ------------ | -------- | -------------------------- |
| id       | number       | 必须     | 用户id                     |
| username | string       | 非必须   | 用户名，最大长度为20字符   |
| password | string       | 非必须   | 密码，最大长度为20字符     |
| role     | string       | 非必须   | 用户角色                   |
| email    | string       | 非必须   | 电子邮箱，最大长度为30字符 |
| age      | unsigned int | 非必须   | 用户年龄                   |
| phone    | string       | 非必须   | 电话号码，最大长度为20字符 |
| address  | string       | 非必须   | 地址，最大长度为30字符     |
| name     | string       | 非必须   | 姓名，最大长度为20字符     |
| image    | string       | 非必须   | 头像，长度为300字符        |

样例：

```json
{
    "code": 1,
    "msg": "success",
    "data": {
        "id": 123,
        "username": "updated_user",
        "password": "afdsa"
        "role": "user",
        "email": "updated@example.com",
        "age": 30,
        "phone": "0987654321",
        "address": "456 Updated St",
        "name": "Jane Doe",
        "updateTime": "2024-03-11T13:00:00",
        "createTime": "2024-03-11T13:00:00",
        "image":""  
    }
}
```

## 4.2 用户个人设置头像

### 4.2.1 请求路径

/info

### 4.2.2 请求方式

POST

### 4.2.3 接口描述

个人设置自己的头像

注意：再传给你url之后，你需要调用update方法将图片url存入数据库

### 4.2.4 请求数据

参数格式：multipart/form-data

参数说明：

| 参数名称 | 参数类型 | 是否必须 | 示例 | 备注 |
| -------- | -------- | -------- | ---- | ---- |
| image    | file     | 是       |      |      |



### 4.2.5 响应数据

格式：json

| 参数名称 | 参数类型 | 是否必传 | 备注                 |
| -------- | -------- | -------- | -------------------- |
| code     | number   | 必须     | 响应码，1成功，0失败 |
| msg      | string   | 非必须   | 提示信息             |
| data     | string   | 必须     | 图片的上传路径       |

```json
{
    "code":1,
    "msg":"success",
    "data":"fhjdauiofgheuwioahfuwie"
}
```

# 5. 功能性接口

## 5.1 老人和志愿者根据活动id查信息

### 5.1.1 请求路径

users/volold/activity

### 5.1.2 请求方式

GET

### 5.1.3 接口描述

根据id查询活动

### 5.1.4 请求数据

格式：queryString

| 参数名 | 类型   | 示例 | 是否必须 | 备注     |
| ------ | ------ | ---- | -------- | -------- |
| id     | number | 1    | 必须     | 活动的id |

### 5.1.5 响应数据

格式：json

| 参数名称           | 参数类型 | 是否必传 | 备注                                                         |
| ------------------ | -------- | -------- | ------------------------------------------------------------ |
| code               | number   | 必须     | 响应码，1成功，0失败                                         |
| msg                | string   | 必须     | 提示信息                                                     |
| data               | object   | 必须     | 返回的数据                                                   |
| \|-total           | number   | 必须     | 总记录数                                                     |
| \|-rows            | object[] | 必须     | 每个活动的数据包装进去                                       |
| \|-id              | number   | 非必须   | 活动的id                                                     |
| \|-title           | string   | 非必须   | 志愿者活动标题                                               |
| \|-quota           | string   | 非必须   | 活动名额                                                     |
| \|-deadline        | string   | 非必须   | 活动报名截止时间                                             |
| \|-date            | string   | 非必须   | 活动日期                                                     |
| \|-begin           | string   | 非必须   | 活动开始时间，时分秒                                         |
| \|-end             | string   | 非必须   | 活动结束时间，时分秒                                         |
| \|-address         | string   | 非必须   | 活动地址                                                     |
| \|-oldId           | number   | 非必须   | (id的大写Id)，发布活动的老人id                               |
| \|-phone           | string   | 非必须   | 发布人的电话号码                                             |
| \|-description     | string   | 非必须   | 对于该志愿活动的描述                                         |
| \|-status          | number   | 非必须   | 1代表未审核的活动，2代表审核同意的活动，3代表审核不同意的活动，4代表已过期的活动 |
| \|-administratorId | number   | 非必须   | 如果审核了，管理员的管理员id                                 |
| \|-createTime      | string   | 非必须   | 创建时间                                                     |
| \|-updateTime      | string   | 非必须   | 更新时间                                                     |
| \|-message         | string   | 非必须   | 管理员对项目的意见，主要对不同意的活动                       |
| \|-remain          | number   | 非必须   | 活动剩余名额                                                 |

样例：

```json
{
    "code":1,
    "msg":"success",
    "data":{            
				"id":"1",
                ...省略了,
                "message":"这个用户活动不对劲"
            }           
}
```

## 
